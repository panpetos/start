/**
 * header-init.js — устаревший инициализатор шапки.
 *
 * Вся логика единой шапки и кружка «Личный кабинет» (включая показ аватарки и
 * переход в кабинет по роли) перенесена в js/layout.js. Этот файл оставлен как
 * безопасная заглушка (no-op), чтобы закэшированные страницы, всё ещё
 * ссылающиеся на него, не подхватывали старую версию со ссылками на /admin.html
 * и не создавали вторую, «раздвоенную» шапку.
 *
 * Ничего не делает: шапкой полностью управляет layout.js.
 */
(function () {
  // no-op — header is fully managed by /js/layout.js
})();
