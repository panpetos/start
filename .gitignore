# Секреты Робокассы — только на сервере, НЕ в git
api/robokassa_config.php

# Токен автоматизации задач — только на сервере, НЕ в git
api/dev_tasks_config.php

# Конфиг каналов уведомлений (токены ботов, SMTP) — только на сервере, НЕ в git
api/notifications_config.php

# Конфиг ИИ-чата (OpenRouter API key) — только на сервере, НЕ в git
api/ai_chat_config.php

# Доступ к TURN-серверу для звонков — только на сервере, НЕ в git
api/rtc_calls_config.php

# Прочие локальные секреты/конфиги вне репозитория
api/config.php
api/db.php
*.env
.env
