<?php
/**
 * sber_return.php — куда банк возвращает человека после успешной оплаты.
 *
 * Зачем отдельный файл, а не ?action=return у sber_acquiring.php: банк дописывает к
 * адресу возврата свои параметры (orderId, lang). Если в адресе уже есть «?», склейка
 * ломается и orderId до нас не доходит — а без него мы не знаем, какой заказ проверять,
 * и оплата не превращается в запись. Путь без вопросительного знака снимает эту
 * зависимость от того, как именно банк склеивает адрес.
 */
$_GET['action'] = 'return';
require __DIR__ . '/sber_acquiring.php';
