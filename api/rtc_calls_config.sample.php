<?php
/**
 * Образец настроек звонков. Рабочий файл — api/rtc_calls_config.php (он в .gitignore,
 * потому что содержит пароль от TURN).
 *
 * Нужен только TURN — ретранслятор для тех пар, где прямое соединение не встаёт
 * (оба за «строгим» NAT: мобильные операторы, корпоративные сети). Без него звонки
 * работают, но у части людей будут срываться на «не удалось соединиться».
 *
 * TURN нельзя поднять на shared-хостинге: нужен сервер с открытыми UDP-портами.
 * Варианты: свой coturn на самом дешёвом VPS либо готовый платный сервис.
 *
 * Пример для coturn со статическим паролем:
 *   turnserver -a -u psytalk:ПАРОЛЬ -r psytalk.pro --no-tls --no-dtls
 */
return [
    'turn_urls' => [
        // 'turn:turn.psytalk.pro:3478?transport=udp',
        // 'turn:turn.psytalk.pro:3478?transport=tcp',
        // 'turn:turn.psytalk.pro:443?transport=tcp',   // где режут всё, кроме 80/443
    ],
    'turn_username'   => '',
    'turn_credential' => '',

    /**
     * Запасной общедоступный ретранслятор (openrelay.metered.ca), пока своего нет.
     * Включён по умолчанию — иначе у части людей звонок не встаёт вовсе.
     *
     * Что важно понимать. Это чужой бесплатный сервис без гарантий доступности:
     * он может тормозить или лечь, и повлиять на это нельзя. Содержание разговора
     * через него не утекает — WebRTC шифрует media между собеседниками (DTLS-SRTP),
     * ретранслятор видит только шифрованные пакеты; но ip-адреса собеседников он
     * видит. Свой coturn на любом дешёвом VPS снимает оба возражения и имеет
     * приоритет над этим запасным.
     */
    'use_public_turn' => true,
];
