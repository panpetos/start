<?php
/**
 * ОБРАЗЕЦ конфигурации эквайринга Сбербанка (задача №72).
 *
 * КАК ПОЛЬЗОВАТЬСЯ:
 *   1) Скопируйте этот файл рядом под именем  sber_acquiring_config.php
 *      (в той же папке /api). Именно его читает sber_acquiring.php.
 *   2) Впишите userName и password — их присылает банк отдельно для теста и для боя.
 *   3) Файл sber_acquiring_config.php НЕ коммитится в git и деплоем не
 *      перезаписывается — ключи остаются только на сервере.
 *   4) Переключение тест/бой — одним флагом is_test.
 *
 * Логин и пароль в переписку НЕ передавайте — впишите их сами прямо на сервере.
 */

return [
    // 1 = тестовый контур (ecomtest.sberbank.ru), 0 = боевой
    'is_test' => 1,

    'test' => [
        'userName' => 'ВПИШИТЕ_ЛОГИН_ОТ_БАНКА',
        'password' => 'ВПИШИТЕ_ПАРОЛЬ_ОТ_БАНКА',
        'base'     => 'https://ecomtest.sberbank.ru/ecomm/gate/rest',
    ],
    'prod' => [
        'userName' => 'ВПИШИТЕ_ЛОГИН_ОТ_БАНКА',
        'password' => 'ВПИШИТЕ_ПАРОЛЬ_ОТ_БАНКА',
        'base'     => 'https://securepayments.sberbank.ru/payment/rest',
    ],

    // Схема расчётов: платформа — агент, деньги за услугу принадлежат исполнителю
    // (самозанятому психологу). Банк требует это указывать в каждом заказе.
    'agent' => [
        // По документации для нашей схемы тип агента — «another»
        'type' => 'another',
        // ИНН и название платформы как агента
        'inn'  => 'ВПИШИТЕ_ИНН_ПЛАТФОРМЫ',
        'name' => 'psytalk.pro',
        // Телефон агента в формате 7XXXXXXXXXX (нужен в чеке)
        'phone' => 'ВПИШИТЕ_ТЕЛЕФОН',
    ],

    // Куда банк вернёт человека после оплаты. Адрес БЕЗ вопросительного знака:
    // банк дописывает к нему свои параметры (orderId), и на адресе с «?» склейка
    // ломается — orderId не доходит, и оплата не превращается в запись.
    // Можно оставить пустыми: тогда подставятся эти же адреса.
    'return_url' => 'https://psytalk.pro/api/sber_return.php',
    'fail_url'   => 'https://psytalk.pro/api/sber_fail.php',

    // Ставка НДС для позиции чека: 0 — «без НДС» (услуги самозанятого)
    'vat' => 0,
];
