<?php
/**
 * ОБРАЗЕЦ конфигурации Робокассы.
 *
 * КАК ПОЛЬЗОВАТЬСЯ:
 *   1) Скопируйте этот файл рядом под именем  robokassa_config.php
 *      (в той же папке /api).  Именно robokassa_config.php читает robokassa.php.
 *   2) Впишите свои 4 пароля (тестовые и боевые).
 *   3) Файл robokassa_config.php НЕ коммитится в git (см. .gitignore) и деплоем
 *      не перезаписывается — ключи остаются только на сервере.
 *   4) Переключение тест/боевой — одним флагом IsTest (1 = тест, 0 = боевой).
 *
 * Пароли в переписку НЕ передавайте — впишите их сами прямо на сервере.
 */

return [
    // Идентификатор магазина в Робокассе
    'MerchantLogin' => 'psytalk.pro',

    // 1 = ТЕСТОВЫЙ режим (IsTest=1), 0 = БОЕВОЙ. Для запуска приёма реальных
    // платежей поменяйте на 0 и убедитесь, что заполнены prod-пароли.
    'IsTest' => 1,

    // Тестовые пароли (из личного кабинета Робокассы, раздел «Технические настройки»)
    'test' => [
        'password1' => 'ВПИШИТЕ_TEST_Password1',
        'password2' => 'ВПИШИТЕ_TEST_Password2',
    ],

    // Боевые пароли
    'prod' => [
        'password1' => 'ВПИШИТЕ_PROD_Password1',
        'password2' => 'ВПИШИТЕ_PROD_Password2',
    ],

    // Фискализация чеков (54-ФЗ). Пока выключено, чтобы тестовые платежи проходили
    // без настройки ОФД. Включите (enabled => true), когда договоритесь с Робокассой
    // о фискализации. Параметры уже под УСН «доход» и предмет расчёта «услуга».
    'receipt' => [
        'enabled'        => false,
        'sno'            => 'usn_income',   // УСН доход
        'payment_method' => 'full_payment', // полный расчёт
        'payment_object' => 'service',      // услуга
        'tax'            => 'none',          // без НДС
    ],
];
