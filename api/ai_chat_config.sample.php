<?php
/**
 * ОБРАЗЕЦ конфигурации ИИ-чата.
 *
 * Настраивается из админки («🤖 ИИ-ассистент»), которая сама записывает рядом файл
 * ai_chat_config.php. Этот образец нужен, только если удобнее завести файл руками.
 * ai_chat_config.php НЕ коммитится в git (см. .gitignore) — ключи в репозитории не хранятся.
 *
 * provider:
 *   'yandex'     — Yandex Cloud Foundation Models (совместимый с OpenAI эндпоинт)
 *   'openrouter' — OpenRouter
 *
 * Для Яндекса нужны ДВЕ вещи: API-ключ и идентификатор каталога (folder_id, вида b1g...).
 * Одного ключа мало: имя модели передаётся как gpt://<folder_id>/<модель>, и без каталога
 * запрос уходит в никуда.
 */

return [
    'provider'  => 'yandex',
    'api_key'   => 'ВПИШИТЕ_КЛЮЧ',
    'folder_id' => 'ВПИШИТЕ_ИДЕНТИФИКАТОР_КАТАЛОГА',

    // Пусто — берётся список по умолчанию из ai_chat.php
    'models' => [],

    // Поиск в интернете (Yandex Search API). Услуга включается отдельно в Yandex Cloud
    // и тарифицируется отдельно от моделей. Пока здесь false, в чате переключатель
    // «Интернет» показывается неактивным и объясняет, почему поиск недоступен.
    //
    // Чтобы включить: в Yandex AI Studio (или консоли Yandex Cloud), в ТОМ ЖЕ каталоге,
    // что в folder_id, подключите Search API, а сервисному аккаунту этого ключа выдайте
    // роли search-api.webSearch.user и search-api.executor. Ключ менять не нужно — поиск
    // ходит тем же Api-Key. Проверить можно кнопкой «🌐 Проверить поиск» в админке.
    //
    // Сначала пробуем генеративный поиск (/v2/gen/search) — он отдаёт готовую выжимку
    // со ссылками. Если он в каталоге не открыт, сами переходим на обычный текстовый
    // поиск (/v2/web/search, ответ — XML в base64), тот же, что в Playground.
    'search_enabled' => false,
];
