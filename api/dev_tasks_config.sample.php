<?php
/**
 * dev_tasks_config.sample.php — шаблон секрета очереди «Задачи Клоду».
 *
 * Реальный файл api/dev_tasks_config.php в git НЕ хранится (см. .gitignore) и
 * обычно создаётся автоматически из админки (вкладка «🤖 Задачи Клоду»).
 * Если создаёте вручную (например, после переезда на ВДС) — скопируйте этот файл
 * в dev_tasks_config.php и впишите свой токен (мин. 16 символов).
 *
 * Токен даёт право читать/обновлять задачи через API и передаётся в промте
 * расписания (Routine), а НЕ хранится в репозитории.
 */
return [
    'token' => 'CHANGE_ME_длинный_случайный_токен_минимум_16_символов',
];
